<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>GuideAI - आपका AI करियर गाइड</title>
  <meta name="description" content="AI-powered education and career guidance in Hindi, English, Arabic, Spanish" />
  <link rel="manifest" href="manifest.json" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Baloo+2:wght@600;700;800&display=swap" rel="stylesheet" />
  <style>
    :root {
      --bg: #0d0f1a;
      --surface: #161929;
      --card: #1e2235;
      --border: #2a3050;
      --accent: #6c63ff;
      --accent2: #ff6b6b;
      --gold: #ffd166;
      --text: #e8eaf6;
      --muted: #8892b0;
      --success: #06d6a0;
      --radius: 16px;
      --font-hi: 'Baloo 2', sans-serif;
      --font-en: 'Inter', sans-serif;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: var(--font-en);
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* HEADER */
    header {
      background: var(--surface);
      border-bottom: 1px solid var(--border);
      padding: 14px 20px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      position: sticky;
      top: 0;
      z-index: 100;
      backdrop-filter: blur(10px);
    }

    .logo {
      font-family: var(--font-hi);
      font-size: 1.5rem;
      font-weight: 800;
      background: linear-gradient(135deg, var(--accent), var(--gold));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .lang-select {
      background: var(--card);
      border: 1px solid var(--border);
      color: var(--text);
      padding: 6px 12px;
      border-radius: 20px;
      font-size: 0.8rem;
      cursor: pointer;
      outline: none;
    }

    /* HERO */
    .hero {
      padding: 40px 20px 30px;
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .hero::before {
      content: '';
      position: absolute;
      width: 300px; height: 300px;
      background: radial-gradient(circle, rgba(108,99,255,0.15), transparent 70%);
      top: -50px; left: 50%;
      transform: translateX(-50%);
      border-radius: 50%;
      pointer-events: none;
    }

    .hero-badge {
      display: inline-block;
      background: rgba(108,99,255,0.15);
      border: 1px solid rgba(108,99,255,0.4);
      color: #a5a0ff;
      padding: 5px 14px;
      border-radius: 20px;
      font-size: 0.75rem;
      font-weight: 600;
      letter-spacing: 0.05em;
      margin-bottom: 16px;
      text-transform: uppercase;
    }

    .hero h1 {
      font-family: var(--font-hi);
      font-size: 2rem;
      font-weight: 800;
      line-height: 1.2;
      margin-bottom: 12px;
    }

    .hero h1 span {
      background: linear-gradient(135deg, var(--accent), var(--gold));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .hero p {
      color: var(--muted);
      font-size: 0.95rem;
      line-height: 1.6;
      max-width: 340px;
      margin: 0 auto 24px;
    }

    /* QUICK TOPICS */
    .section { padding: 0 20px 28px; }

    .section-title {
      font-size: 0.7rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--muted);
      margin-bottom: 14px;
    }

    .topics-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 10px;
    }

    .topic-card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 16px 14px;
      cursor: pointer;
      transition: all 0.2s;
      text-align: left;
    }

    .topic-card:active {
      transform: scale(0.97);
      border-color: var(--accent);
    }

    .topic-icon {
      font-size: 1.6rem;
      margin-bottom: 8px;
    }

    .topic-title {
      font-size: 0.85rem;
      font-weight: 600;
      color: var(--text);
      margin-bottom: 3px;
    }

    .topic-sub {
      font-size: 0.72rem;
      color: var(--muted);
    }

    /* CHAT AREA */
    .chat-section { padding: 0 20px 20px; }

    .chat-box {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      min-height: 200px;
      max-height: 340px;
      overflow-y: auto;
      padding: 16px;
      margin-bottom: 12px;
      scroll-behavior: smooth;
    }

    .chat-placeholder {
      color: var(--muted);
      font-size: 0.85rem;
      text-align: center;
      padding: 40px 20px;
      line-height: 1.6;
    }

    .msg {
      margin-bottom: 14px;
      display: flex;
      flex-direction: column;
    }

    .msg.user { align-items: flex-end; }
    .msg.ai { align-items: flex-start; }

    .msg-bubble {
      max-width: 85%;
      padding: 10px 14px;
      border-radius: 14px;
      font-size: 0.88rem;
      line-height: 1.55;
    }

    .msg.user .msg-bubble {
      background: linear-gradient(135deg, var(--accent), #8b5cf6);
      color: #fff;
      border-bottom-right-radius: 4px;
    }

    .msg.ai .msg-bubble {
      background: var(--surface);
      border: 1px solid var(--border);
      color: var(--text);
      border-bottom-left-radius: 4px;
    }

    .msg-label {
      font-size: 0.65rem;
      color: var(--muted);
      margin-bottom: 4px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    /* INPUT */
    .input-row {
      display: flex;
      gap: 10px;
      align-items: flex-end;
    }

    .chat-input {
      flex: 1;
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 12px 16px;
      color: var(--text);
      font-size: 0.9rem;
      font-family: var(--font-en);
      outline: none;
      resize: none;
      min-height: 48px;
      max-height: 120px;
      transition: border-color 0.2s;
      line-height: 1.4;
    }

    .chat-input:focus { border-color: var(--accent); }
    .chat-input::placeholder { color: var(--muted); }

    .send-btn {
      background: linear-gradient(135deg, var(--accent), #8b5cf6);
      border: none;
      border-radius: 14px;
      width: 48px;
      height: 48px;
      cursor: pointer;
      font-size: 1.2rem;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      transition: opacity 0.2s, transform 0.1s;
    }

    .send-btn:active { transform: scale(0.93); }
    .send-btn:disabled { opacity: 0.5; cursor: not-allowed; }

    /* TYPING INDICATOR */
    .typing {
      display: flex;
      gap: 5px;
      padding: 10px 14px;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 14px;
      border-bottom-left-radius: 4px;
      width: fit-content;
    }

    .typing span {
      width: 7px; height: 7px;
      background: var(--muted);
      border-radius: 50%;
      animation: bounce 1.2s infinite;
    }
    .typing span:nth-child(2) { animation-delay: 0.2s; }
    .typing span:nth-child(3) { animation-delay: 0.4s; }

    @keyframes bounce {
      0%, 60%, 100% { transform: translateY(0); }
      30% { transform: translateY(-6px); }
    }

    /* STATS */
    .stats-row {
      display: flex;
      gap: 10px;
      padding: 0 20px 28px;
    }

    .stat-card {
      flex: 1;
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 14px;
      text-align: center;
    }

    .stat-num {
      font-family: var(--font-hi);
      font-size: 1.5rem;
      font-weight: 800;
      color: var(--gold);
    }

    .stat-label {
      font-size: 0.7rem;
      color: var(--muted);
      margin-top: 2px;
    }

    /* FOOTER */
    footer {
      text-align: center;
      padding: 20px;
      color: var(--muted);
      font-size: 0.75rem;
      border-top: 1px solid var(--border);
    }

    /* SCROLLBAR */
    .chat-box::-webkit-scrollbar { width: 4px; }
    .chat-box::-webkit-scrollbar-track { background: transparent; }
    .chat-box::-webkit-scrollbar-thumb { background: var(--border); border-radius: 2px; }

    /* LOADING OVERLAY */
    #loading-overlay {
      position: fixed;
      inset: 0;
      background: var(--bg);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      z-index: 999;
      transition: opacity 0.5s;
    }

    .loader-logo {
      font-family: var(--font-hi);
      font-size: 2.5rem;
      font-weight: 800;
      background: linear-gradient(135deg, var(--accent), var(--gold));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 20px;
    }

    .loader-bar {
      width: 160px;
      height: 3px;
      background: var(--border);
      border-radius: 2px;
      overflow: hidden;
    }

    .loader-bar-fill {
      height: 100%;
      background: linear-gradient(90deg, var(--accent), var(--gold));
      border-radius: 2px;
      animation: load 1.2s ease forwards;
    }

    @keyframes load {
      from { width: 0%; }
      to { width: 100%; }
    }
  </style>
</head>
<body>

<!-- LOADING -->
<div id="loading-overlay">
  <div class="loader-logo">GuideAI ✨</div>
  <div class="loader-bar"><div class="loader-bar-fill"></div></div>
</div>

<!-- HEADER -->
<header>
  <div class="logo">GuideAI ✨</div>
  <select class="lang-select" id="langSelect" onchange="changeLang(this.value)">
    <option value="hi">🇮🇳 हिंदी</option>
    <option value="en">🇬🇧 English</option>
    <option value="ar">🇸🇦 العربية</option>
    <option value="es">🇪🇸 Español</option>
  </select>
</header>

<!-- HERO -->
<div class="hero">
  <div class="hero-badge" id="badge">AI करियर सहायक</div>
  <h1 id="hero-title">आपका <span>सपना</span>,<br/>हमारी <span>राह</span> 🎯</h1>
  <p id="hero-sub">कोई भी सवाल पूछो — पढ़ाई, करियर, स्कॉलरशिप, या कोर्स — GuideAI आपको सही दिशा देगा।</p>
</div>

<!-- STATS -->
<div class="stats-row">
  <div class="stat-card">
    <div class="stat-num">4+</div>
    <div class="stat-label" id="s1">भाषाएं</div>
  </div>
  <div class="stat-card">
    <div class="stat-num">24/7</div>
    <div class="stat-label" id="s2">उपलब्ध</div>
  </div>
  <div class="stat-card">
    <div class="stat-num">AI</div>
    <div class="stat-label" id="s3">पावर्ड</div>
  </div>
</div>

<!-- QUICK TOPICS -->
<div class="section">
  <div class="section-title" id="topic-heading">जल्दी पूछो</div>
  <div class="topics-grid">
    <div class="topic-card" onclick="quickAsk('करियर चुनने में मदद करो')">
      <div class="topic-icon">🎯</div>
      <div class="topic-title" id="t1">करियर गाइड</div>
      <div class="topic-sub" id="t1s">सही रास्ता खोजो</div>
    </div>
    <div class="topic-card" onclick="quickAsk('स्कॉलरशिप के बारे में बताओ')">
      <div class="topic-icon">🎓</div>
      <div class="topic-title" id="t2">स्कॉलरशिप</div>
      <div class="topic-sub" id="t2s">फ्री पढ़ाई के रास्ते</div>
    </div>
    <div class="topic-card" onclick="quickAsk('कौन सा कोर्स करूं बताओ')">
      <div class="topic-icon">📚</div>
      <div class="topic-title" id="t3">कोर्स सलाह</div>
      <div class="topic-sub" id="t3s">बेस्ट कोर्स ढूंढो</div>
    </div>
    <div class="topic-card" onclick="quickAsk('सरकारी नौकरी की तैयारी कैसे करें')">
      <div class="topic-icon">🏛️</div>
      <div class="topic-title" id="t4">Govt Jobs</div>
      <div class="topic-sub" id="t4s">सरकारी नौकरी</div>
    </div>
  </div>
</div>

<!-- CHAT -->
<div class="chat-section">
  <div class="section-title" id="chat-heading">AI से बात करो</div>
  <div class="chat-box" id="chatBox">
    <div class="chat-placeholder" id="chatPlaceholder">
      💬 ऊपर से कोई topic चुनो<br/>या नीचे अपना सवाल लिखो
    </div>
  </div>
  <div class="input-row">
    <textarea
      class="chat-input"
      id="userInput"
      placeholder="यहाँ लिखो..."
      rows="1"
      onkeydown="handleKey(event)"
      oninput="autoResize(this)"
    ></textarea>
    <button class="send-btn" id="sendBtn" onclick="sendMessage()">➤</button>
  </div>
</div>

<footer>
  GuideAI &copy; 2025 &nbsp;·&nbsp; Made with ❤️ by Viram
</footer>

<script>
  // === LANGUAGE CONFIG ===
  const langs = {
    hi: {
      badge: "AI करियर सहायक",
      heroTitle: `आपका <span>सपना</span>,<br/>हमारी <span>राह</span> 🎯`,
      heroSub: "कोई भी सवाल पूछो — पढ़ाई, करियर, स्कॉलरशिप, या कोर्स — GuideAI आपको सही दिशा देगा।",
      s1:"भाषाएं", s2:"उपलब्ध", s3:"पावर्ड",
      topicHead:"जल्दी पूछो", chatHead:"AI से बात करो",
      placeholder:"यहाँ लिखो...",
      chatPlaceholder:"💬 ऊपर से कोई topic चुनो<br/>या नीचे अपना सवाल लिखो",
      t1:"करियर गाइड", t1s:"सही रास्ता खोजो",
      t2:"स्कॉलरशिप", t2s:"फ्री पढ़ाई के रास्ते",
      t3:"कोर्स सलाह", t3s:"बेस्ट कोर्स ढूंढो",
      t4:"Govt Jobs", t4s:"सरकारी नौकरी",
      q1:"करियर चुनने में मदद करो", q2:"स्कॉलरशिप के बारे में बताओ",
      q3:"कौन सा कोर्स करूं बताओ", q4:"सरकारी नौकरी की तैयारी कैसे करें",
      sysPrompt: `तुम GuideAI हो — एक friendly AI career और education guide। 
तुम हमेशा Hindi में जवाब देते हो जब तक user दूसरी भाषा न मांगे।
तुम्हारा काम है students और युवाओं को:
- सही करियर path चुनने में help करना
- Scholarship और education के बारे में बताना
- Government jobs, competitive exams की guidance देना
- Online courses और skills की सलाह देना
हमेशा friendly, encouraging और practical advice दो। Bullet points use करो।`
    },
    en: {
      badge: "AI Career Assistant",
      heroTitle: `Your <span>Dream</span>,<br/>Our <span>Guidance</span> 🎯`,
      heroSub: "Ask anything — studies, career, scholarships, or courses — GuideAI will point you in the right direction.",
      s1:"Languages", s2:"Available", s3:"Powered",
      topicHead:"Quick Topics", chatHead:"Chat with AI",
      placeholder:"Type your question...",
      chatPlaceholder:"💬 Pick a topic above<br/>or type your question below",
      t1:"Career Guide", t1s:"Find your path",
      t2:"Scholarships", t2s:"Study for free",
      t3:"Course Advice", t3s:"Find best courses",
      t4:"Govt Jobs", t4s:"Government careers",
      q1:"Help me choose a career", q2:"Tell me about scholarships",
      q3:"Which course should I do", q4:"How to prepare for government jobs",
      sysPrompt: `You are GuideAI — a friendly AI career and education guide.
Always respond in English unless user asks otherwise.
Your job is to help students and young people with:
- Choosing the right career path
- Scholarships and education information
- Government jobs and competitive exam guidance
- Online courses and skills advice
Always be friendly, encouraging and give practical advice. Use bullet points.`
    },
    ar: {
      badge: "مساعد المهنة بالذكاء الاصطناعي",
      heroTitle: `حلمك <span>يبدأ</span><br/>من هنا 🎯`,
      heroSub: "اسأل أي شيء — الدراسة، المهنة، المنح الدراسية — GuideAI سيرشدك.",
      s1:"لغات", s2:"متاح", s3:"ذكاء اصطناعي",
      topicHead:"مواضيع سريعة", chatHead:"تحدث مع الذكاء الاصطناعي",
      placeholder:"اكتب سؤالك هنا...",
      chatPlaceholder:"💬 اختر موضوعاً أو اكتب سؤالك",
      t1:"إرشاد مهني", t1s:"ابحث عن طريقك",
      t2:"المنح الدراسية", t2s:"ادرس مجاناً",
      t3:"نصائح الدورات", t3s:"أفضل الدورات",
      t4:"وظائف حكومية", t4s:"مسارات حكومية",
      q1:"ساعدني في اختيار مهنة", q2:"أخبرني عن المنح الدراسية",
      q3:"ما هي الدورة التي يجب أن أختار", q4:"كيف أستعد للوظائف الحكومية",
      sysPrompt:`أنت GuideAI - مساعد ذكاء اصطناعي للتوجيه الوظيفي والتعليمي.
أجب دائماً بالعربية. ساعد الطلاب والشباب في اختيار المهنة والمنح والدورات.`
    },
    es: {
      badge: "Asistente de Carrera IA",
      heroTitle: `Tu <span>Sueño</span>,<br/>Nuestra <span>Guía</span> 🎯`,
      heroSub: "Pregunta cualquier cosa — estudios, carrera, becas — GuideAI te orientará.",
      s1:"Idiomas", s2:"Disponible", s3:"Con IA",
      topicHead:"Temas Rápidos", chatHead:"Habla con la IA",
      placeholder:"Escribe tu pregunta...",
      chatPlaceholder:"💬 Elige un tema arriba<br/>o escribe tu pregunta",
      t1:"Guía de Carrera", t1s:"Encuentra tu camino",
      t2:"Becas", t2s:"Estudia gratis",
      t3:"Consejos de Cursos", t3s:"Mejores cursos",
      t4:"Trabajo Público", t4s:"Empleos gubernamentales",
      q1:"Ayúdame a elegir una carrera", q2:"Cuéntame sobre becas",
      q3:"Qué curso debo hacer", q4:"Cómo prepararme para empleos del gobierno",
      sysPrompt:`Eres GuideAI - un asistente amigable de orientación profesional y educativa con IA.
Responde siempre en español. Ayuda a estudiantes y jóvenes con carrera, becas y cursos.`
    }
  };

  let currentLang = 'hi';
  let messages = [];

  function changeLang(lang) {
    currentLang = lang;
    const L = langs[lang];
    document.getElementById('badge').textContent = L.badge;
    document.getElementById('hero-title').innerHTML = L.heroTitle;
    document.getElementById('hero-sub').textContent = L.heroSub;
    document.getElementById('s1').textContent = L.s1;
    document.getElementById('s2').textContent = L.s2;
    document.getElementById('s3').textContent = L.s3;
    document.getElementById('topic-heading').textContent = L.topicHead;
    document.getElementById('chat-heading').textContent = L.chatHead;
    document.getElementById('userInput').placeholder = L.placeholder;
    document.getElementById('chatPlaceholder').innerHTML = L.chatPlaceholder;
    document.getElementById('t1').textContent = L.t1;
    document.getElementById('t1s').textContent = L.t1s;
    document.getElementById('t2').textContent = L.t2;
    document.getElementById('t2s').textContent = L.t2s;
    document.getElementById('t3').textContent = L.t3;
    document.getElementById('t3s').textContent = L.t3s;
    document.getElementById('t4').textContent = L.t4;
    document.getElementById('t4s').textContent = L.t4s;
    const cards = document.querySelectorAll('.topic-card');
    const qs = [L.q1, L.q2, L.q3, L.q4];
    cards.forEach((c, i) => c.setAttribute('onclick', `quickAsk('${qs[i]}')`));
    document.documentElement.dir = lang === 'ar' ? 'rtl' : 'ltr';
    messages = [];
    document.getElementById('chatBox').innerHTML = `<div class="chat-placeholder">${L.chatPlaceholder}</div>`;
  }

  function quickAsk(q) {
    document.getElementById('userInput').value = q;
    sendMessage();
  }

  function autoResize(el) {
    el.style.height = 'auto';
    el.style.height = Math.min(el.scrollHeight, 120) + 'px';
  }

  function handleKey(e) {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      sendMessage();
    }
  }

  function appendMsg(role, text) {
    const box = document.getElementById('chatBox');
    const ph = box.querySelector('.chat-placeholder');
    if (ph) ph.remove();

    const div = document.createElement('div');
    div.className = `msg ${role}`;
    const label = role === 'user' ? '👤 आप' : '🤖 GuideAI';
    div.innerHTML = `
      <div class="msg-label">${label}</div>
      <div class="msg-bubble">${text.replace(/\n/g,'<br/>')}</div>
    `;
    box.appendChild(div);
    box.scrollTop = box.scrollHeight;
  }

  function showTyping() {
    const box = document.getElementById('chatBox');
    const div = document.createElement('div');
    div.className = 'msg ai';
    div.id = 'typing-indicator';
    div.innerHTML = `
      <div class="msg-label">🤖 GuideAI</div>
      <div class="typing"><span></span><span></span><span></span></div>
    `;
    box.appendChild(div);
    box.scrollTop = box.scrollHeight;
  }

  function removeTyping() {
    const t = document.getElementById('typing-indicator');
    if (t) t.remove();
  }

  async function sendMessage() {
    const input = document.getElementById('userInput');
    const btn = document.getElementById('sendBtn');
    const text = input.value.trim();
    if (!text) return;

    input.value = '';
    input.style.height = 'auto';
    btn.disabled = true;

    appendMsg('user', text);
    messages.push({ role: 'user', content: text });

    showTyping();

    try {
      const L = langs[currentLang];
      const res = await fetch('https://api.anthropic.com/v1/messages', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          model: 'claude-sonnet-4-6',
          max_tokens: 1000,
          system: L.sysPrompt,
          messages: messages
        })
      });
      const data = await res.json();
      const reply = data.content?.[0]?.text || 'माफ करना, कुछ गड़बड़ हुई। फिर कोशिश करो।';
      removeTyping();
      appendMsg('ai', reply);
      messages.push({ role: 'assistant', content: reply });
    } catch(err) {
      removeTyping();
      appendMsg('ai', '⚠️ नेटवर्क की समस्या है। इंटरनेट चेक करो और फिर कोशिश करो।');
    }

    btn.disabled = false;
  }

  // HIDE LOADING
  window.addEventListener('load', () => {
    setTimeout(() => {
      const overlay = document.getElementById('loading-overlay');
      overlay.style.opacity = '0';
      setTimeout(() => overlay.remove(), 500);
    }, 1300);
  });

  // SERVICE WORKER
  if ('serviceWorker' in navigator) {
    navigator.serviceWorker.register('sw.js').catch(() => {});
  }
</script>
</body>
</html>